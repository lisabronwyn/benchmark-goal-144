function stringedIndexes(string, index) {
    let newIndex = string.split('') {
        if (index > newIndex.length) {
            return " "
        } else {
            return newIndex
        }
        if (index < 0) {
            return " "
        }
    }
  }
